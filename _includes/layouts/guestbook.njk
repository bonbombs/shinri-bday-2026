---
layout: layouts/base.njk
---
<link rel="stylesheet" href="{{ "/css/posts.css" }}" />

<header>
    <h2 style="margin: 0;">{{ title }}</h2>
    {# <span><time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time></span> #}
</header>
<div class="post-metadata">
	
	{# {%- for tag in tags | filterTagList %}
	{%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
	<li><a href="{{ tagUrl }}" class="post-tag">{{ tag }}</a>{%- if not loop.last %}, {% endif %}</li>
	{%- endfor %} #}
</div>

<div>
{% set thread = collections.thread | findThread(id) %}
{% for post in collections.postsByThread[id] %}
    {% set userid = post.data.user %}
    {% set userdata = users | findUser(userid) %}
    {% set postcontent = post.content %}
    {% set header %}
        <span class="header-inner">
            <a href="{{post.page.url}}">
            {% if loop.index > 1 %}Re: {% endif %}
            {{ thread.data.title }}</a> by 
            <span><a href="/users/{{userdata.id}}">{{ userdata.username }}</a></span> » 
            <time datetime="{{post.date}}">{{post.date | htmlDateTime }}</time>
        </span>
        <span class="num">#{{loop.index}}</span>
    {% endset %}
    {% include "layouts/guestbook-entry.njk" %}
{% endfor %}
{% if locked %}
<div class="thread-notice">this thread is locked</div>
{% endif %}
</div>
{# {%- set previousPost = page.data | getPreviousCollectionItem %}
{%- set nextPost = page.data | getNextCollectionItem %}
{%- if nextPost or previousPost %}
<ul class="links-nextprev">
	{%- if previousPost %}<li class="links-nextprev-prev">← Previous<br> <a href="{{ previousPost.url }}">{{ previousPost.data.title }}</a></li>{% endif %}
	{%- if nextPost %}<li class="links-nextprev-next">Next →<br><a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a></li>{% endif %}
</ul>
{%- endif %} #}